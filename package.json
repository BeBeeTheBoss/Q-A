{
  "name": "sd-training-feedback-survey",
  "version": "1.0.0",
  "private": true,
  "description": "Training feedback survey with JSON file storage",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
